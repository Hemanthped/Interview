import java.util.Arrays;

public  class IntersectionOfArrays {
    public static void main(String[] args) {
        // int firstArray[] = {1,2,2,1};
        // int secondArray[] = {2,2};

        // Arrays.stream(firstArray).filter(x->Arrays.stream(secondArray).anyMatch(y->y==x)).
        // distinct().forEach(System.out::println);
        StringBuilder s1 = new StringBuilder( "I am in chennai today");
        StringBuilder s2 = new StringBuilder("I will be in Bangalore they after tomorrow");
        
   
     
     StringBuilder str =   s2.replace(13,22,"chennai");

      System.out.println(str.toString());

      

       
       

       
       
    }
}
